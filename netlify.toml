[build]
  command = "npm run build"
  publish = ".next"

# Netlify Next.js runtime'ni avtomatik yoqadi (App Router + API route'lar uchun)
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
